#pragma once

#define WIFI_SSID "your-ssid"
#define WIFI_PASSWORD "your-password"

// Same host:port the PWA itself is viewed at (nginx reverse-proxying to Flask).
#define API_BASE_URL "http://nas-host-or-ip:8890"
